import { MaxWordCountDirective } from './max-word-count.directive';

describe('MaxWordCountDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxWordCountDirective();
    expect(directive).toBeTruthy();
  });
});
