import { IfFalseDirective } from './if-false.directive';

describe('IfFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new IfFalseDirective();
    expect(directive).toBeTruthy();
  });
});
